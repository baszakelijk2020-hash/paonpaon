# RC Platform 20260901 — validator GREEN (2026-09-02)

rc HEAD `b83268f` on `rc/platform-20260901`. **Nothing pushed; nothing merged
to `release-integration-lane-h` or `main`.**

## `pnpm --filter @paon/domain validate:completion` → EXIT 0

```
Completion evidence OK (47 tranche file(s); gated checked ids: 20.1, 20.2,
20.3, 20.4, 20.7, 20.9, 20.11, 20.12, 20.13, 20.14, 20.15, 20.17, 20.18,
20.20, 20.21, 20.22, 20.23, 20.24, 20.25, 20.26, 20.27, 20.28, 20.30, 20.32,
20.33, 20.34, 20.36, 21.1, 21.2, 21.6; HEAD b83268f)
```

## Full gate status at `b83268f`

| Gate                                                                                                                                             | Result                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `validate:completion`                                                                                                                            | **EXIT 0 — GREEN**                                                                                                                                         |
| `supabase test db` (pgTAP)                                                                                                                       | 51 files / 559 tests / 0 fail (checked earlier at `24510d9`; no migration changed since)                                                                   |
| `pnpm lint` / `pnpm typecheck` (12 pkgs each)                                                                                                    | GREEN                                                                                                                                                      |
| customer / retailer / admin production build                                                                                                     | exit 0 (checked at `24510d9`; app code touched since = only `digital-fitting-room/page.tsx` `unoptimized` + `house-memory-advisor-today.spec.ts` fixtures) |
| Focused E2E (customer/retailer/admin sign-out, wardrobe-removal, DFR handoff, roadmap-approval-rls, corporate-tender, navigation, DFR re-proofs) | all green                                                                                                                                                  |
| V3 controller `e17236b` security review                                                                                                          | ACCEPT (`8b204d9`)                                                                                                                                         |
| Global sign-out security review                                                                                                                  | ACCEPT (`9a777cc`)                                                                                                                                         |
| Independent rc integration review                                                                                                                | ACCEPT (`4baf537`)                                                                                                                                         |

## Composition

`rc/platform-20260901` = `release-integration-lane-h` (`ec84ac5`)

- V3 controller candidate `e17236b` (merge `2cb28da`, 0 conflicts)
- global sign-out candidate `9a777cc` (merge `932a8ac`, 0 conflicts)
- 2 small proven fixes:
  - `2c4a627` `fix(customer): load the fitting-room invitation image unoptimized` — Next image-optimizer returned 400 for one external decorative invitation image; `unoptimized` bypasses the proxy. `customer-navigation-v3-copy.spec.ts` 8/8.
  - `674959a` `test(retailer): fix 3 R0.4 house-memory-advisor-today fixture/assertion bugs` — 3 assertions in the spec (from V3-controller `c74bea2`) never matched the shipped Self-Portrait UI. `house-memory-advisor-today.spec.ts` 3/3.
- evidence closure: 47 tranche files, every checked Stage-20/21 item re-proven at rc HEAD with a path-verified tranche; R2/R3 stale run-SHAs re-stamped (incl. `17.8`/`17.9`/`8.4` confirmed passing now that retailer sign-out is fixed); R4 VWS tranches repointed to the V3 `digital-fitting-room-*-v3` re-proof specs the V3 controller shipped as replacements for the pre-relocation `virtual-studio*.spec.ts`.

## PHASE.md

42 items now `[x]`, every one validator-backed at `b83268f`.

## The single non-green item — 17.14, honestly `[ ]`

`17.14 Prospect AI conversation, buying-intent queue, and human handoff` —
checkbox `[ ]`, tranche `status: implemented_unverified`. The capability is
built (domain `conversation-intent`, migrations `20260806010000` /
`20260808170000`, repositories, the customer storefront inquiry route, the
retailer triage UI, `supabase/tests/conversation_ai_handoff_test.sql`), but
its browser proof `apps/retailer/e2e/prospect-ai-conversation.spec.ts` is a
cross-app journey that needs BOTH the customer (`:3002`) and retailer app
servers running — `apps/retailer/playwright.config.ts` starts only one, and
the spec's `RETAILER_APP_ORIGIN` drifted to `:3011` vs the config's `:3001`
(commit `964d9db`). Building a dual-app Playwright harness is out of scope for
this integration. Pre-existing (`GROUND_TRUTH.md` §3); not integration-caused.
The validator does not gate it (checkbox is `[ ]`), so `validate:completion`
is green.

## Next — founder decision

This rc is green and independently reviewed. The remaining action is outward
and belongs to the founder:

1. Review `rc/platform-20260901` (this worktree / branch).
2. Integrate it — either fast-forward `release-integration-lane-h` to
   `b83268f`, or open `rc/platform-20260901` → `main` and run CI
   (`verify` → `deploy` → `production-gate`).
3. Follow-ups tracked, not blocking: the `17.14` dual-app e2e harness; the
   `apps/customer/tsconfig.json` `.next-e2e-*` regression in the PROTECTED
   checkout (`/Users/nguyen/Projects/PAON` — off-limits to this pipeline);
   Supabase CLI v2.116 bump so `database.types.ts` can be regenerated without
   whitespace churn; the superseded `virtual-studio*.spec.ts` files can be
   deleted (they're replaced by `digital-fitting-room-*-v3.spec.ts`).

Nothing in this pipeline was pushed or merged.
