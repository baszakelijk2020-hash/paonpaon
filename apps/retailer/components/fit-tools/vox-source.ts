// GENERATED — do not edit by hand.
// Extracted verbatim from downloaded_pages/pag1.html by the command in
// docs/DESIGN_PORTS.md. The founder's CSS and JS are the canonical design
// (ADR-052); the only addition is one CustomEvent dispatch marked in the
// script as the PAON data hook. Regenerate rather than edit.

export const VOX_CSS =
  '\n.vox-widget-root {\n  all: initial;\n  display: block;\n  isolation: isolate;\n  contain: layout style paint;\n  font-family: "Optimaklein", sans-serif;\n  width: 390px;\n  margin: 0;\n  padding: 0;\n  background: transparent;\n}\n.vox-widget-root * {\n  all: revert;\n  box-sizing: border-box;\n  -webkit-tap-highlight-color: transparent;\n  margin: 0;\n  padding: 0;\n  user-select: none;\n  -webkit-user-select: none;\n}\n@font-face {\n  font-family: "Optimaklein";\n  src: url("https://www.nebelspiegel.com/fonts/optimaklein.woff2") format("woff2");\n  font-weight: normal;\n  font-style: normal;\n  font-display: swap;\n}\n.vox-widget-root .vox-widget-container {\n  font-family: "Optimaklein", sans-serif;\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  gap: 10px;\n  width: 390px;\n  padding-top: 0;\n  padding-bottom: 0;\n}\n.vox-widget-root .vox-outer {\n  width: 350px;\n}\n.vox-widget-root .vox-slider-row {\n  background: #E8E8E8;\n  border-radius: 8px;\n  padding: 4px 8px;\n  position: relative;\n  overflow: hidden;\n  touch-action: pan-y;\n  transition: background 0.5s ease;\n}\n.vox-widget-root .vox-slider-row.vox-active-row {\n  background: linear-gradient(90deg, #333333 0%, #000000 100%);\n  transition: background 0.5s ease;\n}\n.vox-widget-root .vox-row-header {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  margin-bottom: 1px;\n  pointer-events: none;\n  position: relative;\n  z-index: 3;\n}\n.vox-widget-root .vox-row-side-label {\n  font-size: 10px;\n  color: #aaaaaa;\n  flex: 1;\n  white-space: nowrap;\n  transition: color 0.3s;\n}\n.vox-widget-root .vox-slider-row.vox-active-row .vox-row-side-label {\n  color: rgba(255,255,255,0.5);\n}\n.vox-widget-root .vox-left-label {\n  text-align: left;\n}\n.vox-widget-root .vox-right-label {\n  text-align: right;\n}\n.vox-widget-root .vox-row-name {\n  font-size: 10px;\n  font-weight: normal;\n  color: #333;\n  text-align: center;\n  flex: 2;\n  padding: 0 4px;\n  white-space: nowrap;\n  transition: color 0.3s;\n}\n.vox-widget-root .vox-slider-row.vox-active-row .vox-row-name {\n  color: #fff;\n}\n.vox-widget-root .vox-track-container {\n  position: relative;\n  width: 100%;\n  height: 44px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  pointer-events: none;\n  z-index: 3;\n}\n.vox-widget-root .vox-selection-window {\n  position: absolute;\n  width: 46px;\n  height: 40px;\n  border-radius: 10px;\n  z-index: 10;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  font-size: 10px;\n  font-weight: 600;\n  pointer-events: none;\n  font-family: "Optimaklein", sans-serif;\n}\n.vox-widget-root .vox-viewport {\n  width: 100%;\n  height: 100%;\n  overflow: hidden;\n  -webkit-mask-image: linear-gradient(to right, transparent, black 30%, black 70%, transparent);\n  mask-image: linear-gradient(to right, transparent, black 30%, black 70%, transparent);\n  position: relative;\n  z-index: 3;\n}\n.vox-widget-root .vox-track {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  height: 100%;\n  position: absolute;\n  will-change: transform;\n}\n.vox-widget-root .vox-chip {\n  border: none;\n  appearance: none;\n  -webkit-appearance: none;\n  flex: 0 0 46px;\n  height: 40px;\n  border-radius: 10px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  font-size: 10px;\n  font-weight: 600;\n  background: #fff;\n  color: #808080;\n  cursor: pointer;\n  pointer-events: auto;\n  font-family: "Optimaklein", sans-serif;\n}\n.vox-widget-root .vox-voice-overlay-fixed {\n  position: fixed;\n  inset: 0;\n  z-index: 99999;\n  pointer-events: none;\n  background: transparent !important;\n}\n.vox-widget-root .vox-voice-overlay-fixed.active {\n  pointer-events: none;\n}\n.vox-widget-root .vox-spotlight,\n.vox-widget-root .vox-spotlight2 {\n  display: none !important;\n}\n.vox-widget-root .vox-record-btn-wrap {\n  position: sticky;\n  bottom: 20px;\n  width: 390px;\n  align-self: center;\n  display: flex;\n  justify-content: center;\n  z-index: 1000;\n  pointer-events: none;\n  margin-top: 20px;\n}\n.vox-widget-root .vox-pill {\n  position: relative;\n  width: 60px;\n  height: 60px;\n  border-radius: 30px;\n  background: rgba(255,255,255,0.10);\n  -webkit-backdrop-filter: blur(16px);\n  backdrop-filter: blur(16px);\n  display: flex;\n  align-items: center;\n  justify-content: flex-end;\n  overflow: hidden;\n  pointer-events: auto;\n  box-shadow: 0 4px 24px rgba(0,0,0,0);\n  transition: width 1s cubic-bezier(0.4,0,0.2,1), border-radius 1s cubic-bezier(0.4,0,0.2,1), box-shadow 1s cubic-bezier(0.4,0,0.2,1);\n}\n.vox-widget-root .vox-pill.listening {\n  width: 350px;\n  border-radius: 30px;\n  box-shadow: 0 4px 24px rgba(0,0,0,0.18);\n}\n.vox-widget-root .vox-pill-transcript {\n  position: absolute;\n  left: 20px;\n  top: 0;\n  width: 270px;\n  height: 60px;\n  display: flex;\n  align-items: center;\n  justify-content: flex-start;\n  padding: 0;\n  opacity: 0;\n  transition: opacity 0.4s ease 0.6s;\n  pointer-events: none;\n  overflow: hidden;\n}\n.vox-widget-root .vox-pill.listening .vox-pill-transcript {\n  opacity: 1;\n}\n.vox-widget-root .vox-transcript {\n  font-family: "Optimaklein", sans-serif;\n  font-size: 20px;\n  color: #333;\n  text-align: left;\n  white-space: nowrap;\n  overflow: hidden;\n  max-width: 270px;\n}\n.vox-widget-root .vox-record-btn {\n  position: relative;\n  flex-shrink: 0;\n  width: 60px;\n  height: 60px;\n  border-radius: 50%;\n  border: none;\n  background: linear-gradient(90deg, #808080 0%, #000000 100%);\n  cursor: pointer;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  outline: none;\n  -webkit-appearance: none;\n  pointer-events: auto;\n  transition: background 0.3s;\n  box-shadow: 0 2px 6px rgba(0,0,0,0.2);\n  z-index: 2;\n}\n.vox-widget-root .vox-record-btn.listening {\n  background: linear-gradient(90deg, #ED1C27 0%, #C4101F 100%);\n  animation: vox-pulse 1.4s infinite;\n}\n@keyframes vox-pulse {\n  0%   { box-shadow: 0 0 0 0 rgba(237,28,39,0.35); }\n  70%  { box-shadow: 0 0 0 16px rgba(237,28,39,0); }\n  100% { box-shadow: 0 0 0 0 rgba(237,28,39,0); }\n}\n.vox-widget-root .vox-record-btn svg {\n  width: 22px;\n  height: 22px;\n  fill: rgba(255,255,255,0.9);\n}\n';

export const VOX_SCRIPT =
  "\n(function() {\n  var roots = document.querySelectorAll('[data-vox-widget]');\n  if (!roots.length) return;\n\n  roots.forEach(function(root, rootIndex) {\n    if (root.__voxInitialized) return;\n    root.__voxInitialized = true;\n\n    var container = root.querySelector('.vox-widget-container');\n    var voiceOverlay = root.querySelector('.vox-voice-overlay-fixed');\n\n    var generateVals = function(limit) {\n      var a = [];\n      for (var v = -limit; v <= limit; v += 0.5) a.push(parseFloat(v.toFixed(1)));\n      return a;\n    };\n\n    var TOP_GROUP = [\n      { name: \"Neiging\", left: \"Achterwaarts\", right: \"Voorwaarts\", vals: generateVals(3),\n        matchFn: function(t) { var w = t.split(' ').pop(); return /[aei]ng$|ging$|neiging/i.test(w) || /neiging|nee\\s?ging/i.test(t); } },\n      { name: \"Kraag\", left: \"Kraagplooi\", right: \"Hoger\", vals: generateVals(4),\n        matchFn: function(t) { var w = t.split(' ').pop(); return /[a-z]*aag$|[a-z]*ag$/i.test(w) || /kraag|krach|krag\\b|krijg/i.test(t); } },\n      { name: \"Schouder R\", left: \"Lager\", right: \"Hoger\", vals: generateVals(2),\n        matchFn: function(t) { return /rechts$|recht$|schouder\\s*(r|re|rechts)/i.test(t); } },\n      { name: \"Schouder L\", left: \"Lager\", right: \"Hoger\", vals: generateVals(2),\n        matchFn: function(t) { return /links$|kinks$|ink$|schouder\\s*(l|li|links)/i.test(t); } },\n      { name: \"Sluitknoop\", left: \"Lager\", right: \"Hoger\", vals: generateVals(2),\n        matchFn: function(t) { var w = t.split(' ').pop(); return /knoop$|knop$|noop$|knoep$|sloop$|snoop$|slop$|klop$|oop$/i.test(w) || /sluitknoop|slijt/i.test(t); } },\n      { name: \"Armsgat\", left: \"Lager\", right: \"Hoger\", vals: generateVals(2),\n        matchFn: function(t) { var w = t.split(' ').pop(); return /schat$|gat$|at$/i.test(w) || /armsgat|armschat/i.test(t); } },\n      { name: \"Mouwpositie\", left: \"Voorwaarts\", right: \"Achterwaarts\", vals: generateVals(1),\n        matchFn: function(t) { var w = t.split(' ').pop(); return /positie$|zie$/i.test(w) || /mouwpositie|mouw/i.test(t); } }\n    ];\n\n    var DIR_LABEL = {\n      \"Neiging\":     { pos: \"Voorwaarts\",   neg: \"Achterwaarts\" },\n      \"Kraag\":       { pos: \"Hoger\",        neg: \"Kraagplooi\"   },\n      \"Schouder R\":  { pos: \"Hoger\",        neg: \"Lager\"        },\n      \"Schouder L\":  { pos: \"Hoger\",        neg: \"Lager\"        },\n      \"Sluitknoop\":  { pos: \"Hoger\",        neg: \"Lager\"        },\n      \"Armsgat\":     { pos: \"Hoger\",        neg: \"Lager\"        },\n      \"Mouwpositie\": { pos: \"Achterwaarts\", neg: \"Voorwaarts\"   }\n    };\n\n    var CHIP_W = 46, GAP = 6, STEP = CHIP_W + GAP;\n    var sliderState = {};\n    var transcriptEl = null;\n    var overlayActiveIdx = null;\n    var activeRowTimer = null;\n    var applyDebounceTimer = null;\n    var applyDebounceCtx = null;\n\n    function isWholeNumber(v) {\n      return v !== null && Math.abs(v % 1) < 0.001;\n    }\n\n    function cancelApplyDebounce() {\n      if (applyDebounceTimer) {\n        clearTimeout(applyDebounceTimer);\n        applyDebounceTimer = null;\n      }\n      applyDebounceCtx = null;\n    }\n\n    function fmt(v) {\n      return v === 0 ? '0' : (v > 0 ? '+' : '') + v.toFixed(1);\n    }\n\n    function setTranscript(text) {\n      if (!transcriptEl) transcriptEl = root.querySelector('.vox-transcript');\n      if (!transcriptEl) return;\n      transcriptEl.textContent = text;\n      if (!text) {\n        transcriptEl.style.fontSize = '20px';\n        return;\n      }\n      var size = 20;\n      transcriptEl.style.fontSize = size + 'px';\n      while (transcriptEl.scrollWidth > 258 && size > 9) {\n        size -= 0.5;\n        transcriptEl.style.fontSize = size + 'px';\n      }\n    }\n\n    function activateRow(idx) {\n      clearTimeout(activeRowTimer);\n      for (var id in sliderState) sliderState[id].rowEl.classList.remove('vox-active-row');\n      sliderState[idx].rowEl.classList.add('vox-active-row');\n    }\n\n    function activateRowBoth(idxA, idxB) {\n      clearTimeout(activeRowTimer);\n      for (var id in sliderState) sliderState[id].rowEl.classList.remove('vox-active-row');\n      sliderState[idxA].rowEl.classList.add('vox-active-row');\n      sliderState[idxB].rowEl.classList.add('vox-active-row');\n    }\n\n    function deactivateRows(delay) {\n      clearTimeout(activeRowTimer);\n      activeRowTimer = setTimeout(function() {\n        for (var id in sliderState) sliderState[id].rowEl.classList.remove('vox-active-row');\n      }, delay || 0);\n    }\n\n    TOP_GROUP.forEach(function(cfg, idx) {\n      var vals = cfg.vals;\n      var outer = document.createElement('div');\n      outer.className = 'vox-outer';\n      outer.innerHTML =\n        '<div class=\"vox-slider-row\" role=\"group\" data-field=\"' + cfg.name + '\" aria-label=\"' + cfg.name + '\" data-selected-value=\"\">' +\n          '<div class=\"vox-row-header\">' +\n            '<span class=\"vox-row-side-label vox-left-label\">' + cfg.left + '</span>' +\n            '<span class=\"vox-row-name\">' + cfg.name + '</span>' +\n            '<span class=\"vox-row-side-label vox-right-label\">' + cfg.right + '</span>' +\n          '</div>' +\n          '<div class=\"vox-track-container\">' +\n            '<div class=\"vox-selection-window\">0</div>' +\n            '<div class=\"vox-viewport\"><div class=\"vox-track\"></div></div>' +\n          '</div>' +\n        '</div>';\n\n      container.appendChild(outer);\n\n      var tr = outer.querySelector('.vox-track');\n      var vp = outer.querySelector('.vox-viewport');\n      var sw = outer.querySelector('.vox-selection-window');\n      var row = outer.querySelector('.vox-slider-row');\n\n      // \u2500\u2500 Physics state \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n      var curX = 0;\n      var isDragging = false;\n      var rafId = null;\n\n      var velSamples = [];\n      var MAX_SAMPLES = 6;\n\n      var startX = 0;\n      var startPX = 0;\n\n      var touchStartX = 0, touchStartY = 0, touchDirectionLocked = null;\n\n      // \u2500\u2500 Wheel / trackpad state \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n      var wheelRafId = null;\n      var wheelAccum = 0;   // accumulated deltaX pixels not yet committed\n\n      function getCenter() {\n        return vp.offsetWidth / 2 - CHIP_W / 2;\n      }\n\n      function getBounds() {\n        return {\n          min: getCenter() - (vals.length - 1) * STEP,\n          max: getCenter()\n        };\n      }\n\n      function swColor(v) {\n        if (v === 0) return { bg: '#fff', color: '#808080', fs: '10px' };\n        if (v > 0) return { bg: 'linear-gradient(180deg, #009144 0%, #007A3A 100%)', color: '#fff', fs: '16px' };\n        return { bg: 'linear-gradient(180deg, #ED1C27 0%, #C9101C 100%)', color: '#fff', fs: '16px' };\n      }\n\n      function getNearestIndexFromX(x) {\n        return Math.max(0, Math.min(vals.length - 1, Math.round((getCenter() - x) / STEP)));\n      }\n\n      function update(x, dragging) {\n        tr.style.webkitTransform = 'translateX(' + x + 'px)';\n        tr.style.transform = 'translateX(' + x + 'px)';\n        var i = getNearestIndexFromX(x);\n        var v = vals[i];\n        var sc = swColor(v);\n        sw.textContent = fmt(v);\n        row.setAttribute('data-selected-value', fmt(v));\n        sw.style.background = sc.bg;\n        sw.style.color = sc.color;\n        sw.style.fontSize = sc.fs;\n\n        var chips = tr.querySelectorAll('.vox-chip');\n        for (var ci = 0; ci < chips.length; ci++) {\n          var c = chips[ci], cv = vals[ci], iz = Math.abs(cv) < 0.001;\n          if (dragging) {\n            c.style.background = iz ? 'transparent' : (cv > 0 ? '#BCD4C6' : '#FFE6E6');\n            c.style.color = iz ? '#808080' : (cv > 0 ? '#009144' : '#ED1C27');\n          } else {\n            c.setAttribute('aria-pressed', ci === i ? 'true' : 'false');\n            if (ci === i) {\n              var csc = swColor(cv);\n              c.style.background = csc.bg;\n              c.style.color = csc.color;\n            } else {\n              c.style.background = '#fff';\n              c.style.color = '#808080';\n            }\n          }\n        }\n      }\n\n      // \u2500\u2500 CSS-transition snap \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n      function snapToIdx(i, dur) {\n        if (i < 0 || i >= vals.length) return;\n        if (rafId) { cancelAnimationFrame(rafId); rafId = null; }\n        curX = getCenter() - i * STEP;\n        tr.style.webkitTransition = 'transform ' + dur + 'ms cubic-bezier(0.23,1,0.32,1)';\n        tr.style.transition      = 'transform ' + dur + 'ms cubic-bezier(0.23,1,0.32,1)';\n        update(curX, false);\n        setTimeout(function() {\n          tr.style.webkitTransition = '';\n          tr.style.transition = '';\n        }, dur);\n      }\n\n      function snapToValue(v, dur) {\n        var i = vals.findIndex(function(x) { return Math.abs(x - v) < 0.001; });\n        if (i !== -1) snapToIdx(i, dur || 400);\n      }\n\n      // \u2500\u2500 RAF momentum \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n      function startMomentum(releaseVelocity) {\n        if (rafId) { cancelAnimationFrame(rafId); rafId = null; }\n\n        tr.style.webkitTransition = '';\n        tr.style.transition = '';\n\n        var bounds = getBounds();\n\n        var DECEL_DIST = 28;\n        var rawTarget = curX + releaseVelocity * DECEL_DIST;\n        rawTarget = Math.max(bounds.min, Math.min(bounds.max, rawTarget));\n\n        var snapIdx = getNearestIndexFromX(rawTarget);\n        var targetX = getCenter() - snapIdx * STEP;\n\n        var STIFFNESS = 0.10;\n        var lastFrame = performance.now();\n\n        function step(now) {\n          var dt = Math.min(now - lastFrame, 32);\n          lastFrame = now;\n\n          var remaining = targetX - curX;\n          var delta = remaining * (1 - Math.pow(1 - STIFFNESS, dt / 16.667));\n          curX += delta;\n\n          if (curX < bounds.min) curX = bounds.min;\n          if (curX > bounds.max) curX = bounds.max;\n\n          update(curX, true);\n\n          if (Math.abs(remaining) < 0.3) {\n            rafId = null;\n            curX = targetX;\n            update(curX, false);\n            return;\n          }\n\n          rafId = requestAnimationFrame(step);\n        }\n\n        rafId = requestAnimationFrame(step);\n      }\n\n      // \u2500\u2500 Wheel / trackpad handler \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n      // deltaX: trackpad horizontal swipe (native, pixel-accurate on Mac)\n      // deltaY on mouse: treated as horizontal scroll (scroll wheel)\n      // One STEP of scroll = one chip width, snaps after settling.\n      (function() {\n        var wheelSnapTimer = null;\n\n        row.addEventListener('wheel', function(e) {\n          e.preventDefault();\n\n          // Cancel any ongoing momentum / transition\n          if (rafId) { cancelAnimationFrame(rafId); rafId = null; }\n          if (wheelRafId) { cancelAnimationFrame(wheelRafId); wheelRafId = null; }\n          tr.style.webkitTransition = '';\n          tr.style.transition = '';\n\n          // Use deltaX for trackpad horizontal; fall back to deltaY for mouse wheel.\n          // Normalise: DOM_DELTA_LINE (detail) \u2192 multiply by ~20; DOM_DELTA_PAGE \u2192 multiply by 300.\n          var raw = e.deltaX !== 0 ? e.deltaX : e.deltaY;\n          var unit = e.deltaMode;\n          if (unit === 1) raw *= 20;       // line mode (Firefox mouse)\n          else if (unit === 2) raw *= 300; // page mode\n\n          // Invert: scrolling right (positive deltaX) should move track left \u2192 negative curX delta\n          wheelAccum -= raw;\n\n          var bounds = getBounds();\n          curX = Math.max(bounds.min, Math.min(bounds.max, curX + wheelAccum * 0.4));\n          wheelAccum = 0;\n\n          update(curX, true);\n\n          // After scroll settles, snap to nearest chip\n          clearTimeout(wheelSnapTimer);\n          wheelSnapTimer = setTimeout(function() {\n            var snapIdx = getNearestIndexFromX(curX);\n            snapToIdx(snapIdx, 300);\n          }, 120);\n        }, { passive: false });\n      })();\n\n      // \u2500\u2500 Build chips \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n      vals.forEach(function(v) {\n        var c = document.createElement('button');\n        c.type = 'button';\n        c.className = 'vox-chip';\n        c.textContent = fmt(v);\n        c.setAttribute('data-value', String(v));\n        c.setAttribute('aria-pressed', 'false');\n        c.addEventListener('click', function(e) {\n          e.stopPropagation();\n          if (rafId) { cancelAnimationFrame(rafId); rafId = null; }\n          snapToValue(v, 600);\n          // PAON data hook (ADR-052). A tap is an APPLIED value, exactly as a\n          // spoken one is: without this the slider moves, the tailor sees the\n          // number they asked for, and nothing is ever recorded. The voice\n          // path emitted this from applyValue(); the tap path never did.\n          root.dispatchEvent(new CustomEvent('vox:apply', {\n            detail: { field: cfg.name, value: v },\n            bubbles: true\n          }));\n        });\n        tr.appendChild(c);\n      });\n\n      sliderState[idx] = { snapToValue: snapToValue, cfg: cfg, vals: vals, rowEl: row, swEl: sw };\n\n      // \u2500\u2500 Pointer helpers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n      function getTouchX(e) {\n        if (e.touches && e.touches.length > 0) return e.touches[0].pageX;\n        if (e.changedTouches && e.changedTouches.length > 0) return e.changedTouches[0].pageX;\n        return e.pageX || 0;\n      }\n      function getTouchY(e) {\n        if (e.touches && e.touches.length > 0) return e.touches[0].pageY;\n        if (e.changedTouches && e.changedTouches.length > 0) return e.changedTouches[0].pageY;\n        return e.pageY || 0;\n      }\n\n      // \u2500\u2500 Drag handlers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n      function onStart(e) {\n        if (rafId) { cancelAnimationFrame(rafId); rafId = null; }\n        tr.style.webkitTransition = '';\n        tr.style.transition = '';\n\n        isDragging = true;\n        touchDirectionLocked = null;\n\n        touchStartX = getTouchX(e);\n        touchStartY = getTouchY(e);\n        startPX = touchStartX;\n        startX  = curX;\n\n        velSamples = [{ t: performance.now(), x: touchStartX }];\n      }\n\n      function onMove(e) {\n        if (!isDragging) return;\n        var px = getTouchX(e), py = getTouchY(e);\n        var dx = Math.abs(px - touchStartX), dy = Math.abs(py - touchStartY);\n\n        if (touchDirectionLocked === null && (dx > 6 || dy > 6)) {\n          touchDirectionLocked = (dx > dy * 1.8) ? 'horizontal' : 'vertical';\n        }\n        if (e.touches) {\n          if (touchDirectionLocked === 'vertical') { isDragging = false; return; }\n          if (touchDirectionLocked !== 'horizontal') return;\n          e.preventDefault();\n        }\n\n        var now = performance.now();\n        velSamples.push({ t: now, x: px });\n        if (velSamples.length > MAX_SAMPLES) velSamples.shift();\n\n        curX = startX + (px - startPX);\n        var bounds = getBounds();\n\n        if (curX < bounds.min) {\n          var over = bounds.min - curX;\n          curX = bounds.min - over * 0.25;\n        } else if (curX > bounds.max) {\n          var over2 = curX - bounds.max;\n          curX = bounds.max + over2 * 0.25;\n        }\n\n        update(curX, true);\n      }\n\n      function onEnd() {\n        if (!isDragging) return;\n        isDragging = false;\n\n        var bounds = getBounds();\n        curX = Math.max(bounds.min, Math.min(bounds.max, curX));\n\n        var now = performance.now();\n        var releaseVelocity = 0;\n        if (velSamples.length >= 2) {\n          var windowMs = 80;\n          var ref = null;\n          for (var s = 0; s < velSamples.length; s++) {\n            if (now - velSamples[s].t <= windowMs) { ref = velSamples[s]; break; }\n          }\n          if (!ref) ref = velSamples[0];\n          var dt = now - ref.t;\n          if (dt > 0) {\n            releaseVelocity = (velSamples[velSamples.length - 1].x - ref.x) / dt;\n            releaseVelocity *= 16.667;\n          }\n        }\n\n        startMomentum(releaseVelocity);\n      }\n\n      row.addEventListener('mousedown', onStart, false);\n      window.addEventListener('mousemove', function(e) { if (isDragging) onMove(e); }, false);\n      window.addEventListener('mouseup', onEnd, false);\n      row.addEventListener('touchstart', onStart, { passive: true });\n      window.addEventListener('touchmove', function(e) { if (isDragging) onMove(e); }, { passive: false });\n      window.addEventListener('touchend', onEnd, { passive: true });\n\n      setTimeout(function() {\n        curX = getCenter() - vals.indexOf(0) * STEP;\n        update(curX, false);\n      }, 50);\n    });\n\n    // \u2500\u2500 Voice pill \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    (function() {\n      var btnWrap = document.createElement('div');\n      btnWrap.className = 'vox-record-btn-wrap';\n\n      var pill = document.createElement('div');\n      pill.className = 'vox-pill';\n\n      var pillTranscript = document.createElement('div');\n      pillTranscript.className = 'vox-pill-transcript';\n\n      var transcriptDiv = document.createElement('div');\n      transcriptDiv.className = 'vox-transcript';\n      transcriptDiv.textContent = '';\n      pillTranscript.appendChild(transcriptDiv);\n      transcriptEl = transcriptDiv;\n\n      var btn = document.createElement('button');\n      btn.className = 'vox-record-btn';\n      btn.innerHTML = '<svg viewBox=\"0 0 24 24\"><path d=\"M12 14a3 3 0 0 0 3-3V5a3 3 0 0 0-6 0v6a3 3 0 0 0 3 3zm5-3a5 5 0 0 1-10 0H5a7 7 0 0 0 6 6.92V20H9v2h6v-2h-2v-2.08A7 7 0 0 0 19 11h-2z\"/></svg>';\n      btn.addEventListener('click', function() {\n        voxToggleRecording();\n      });\n\n      pill.appendChild(pillTranscript);\n      pill.appendChild(btn);\n      btnWrap.appendChild(pill);\n      container.appendChild(btnWrap);\n\n      var botSpacer = document.createElement('div');\n      botSpacer.style.cssText = 'width:100%;height:20px;flex-shrink:0;';\n      container.appendChild(botSpacer);\n    })();\n\n    // \u2500\u2500 Voice logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    var voiceActive = false;\n    var rec = null;\n    var isLocked = false;\n    var pendingIdx = null;\n    var accText = '';\n    var pendingSchouderVal = null;\n    var lastAppliedIdx = null;\n    var graceIdx = null;\n    var graceTimer = null;\n\n    var previousValues = {};\n    TOP_GROUP.forEach(function(cfg, idx) {\n      previousValues[idx] = 0;\n    });\n\n    function showOverlay(idx) {\n      overlayActiveIdx = idx;\n      activateRow(idx);\n    }\n\n    function showOverlayBoth(idxA, idxB) {\n      overlayActiveIdx = idxA;\n      activateRowBoth(idxA, idxB);\n    }\n\n    function hideOverlay() {\n      overlayActiveIdx = null;\n      deactivateRows(500);\n    }\n\n    function flashRow(idx, color) {\n      var el = sliderState[idx].rowEl, count = 0;\n      var toggle = function() {\n        el.style.transition = 'background 0.08s';\n        el.style.background = count % 2 === 0 ? color : '#E8E8E8';\n        count++;\n        if (count < 6) setTimeout(toggle, 100);\n        else {\n          el.style.transition = '';\n          el.style.background = '';\n          el.classList.remove('vox-active-row');\n        }\n      };\n      toggle();\n    }\n\n    function parseValue(t) {\n      var s = t\n        .replace(/(\\d+)\\s*\\u00BD/g, function(_, n) { return (parseFloat(n) + 0.5).toString(); })\n        .replace(/\\u00BD/g, '0.5')\n        .replace(/(\\d+)\\s+1\\/2/g, function(_, n) { return (parseFloat(n) + 0.5).toString(); })\n        .replace(/\\b1\\/2\\b/g, '0.5')\n        .replace(/\\b(een|\u00e9\u00e9n|1)\\s+en\\s+(een|\u00e9\u00e9n)\\s+half\\b/gi, '1.5')\n        .replace(/\\banderhalf\\b/gi, '1.5')\n        .replace(/\\bnul\\s*(komma|punt)\\s*(vijf|5)\\b/gi, '0.5')\n        .replace(/\\b(een|\u00e9\u00e9n)\\s*(komma|punt)\\s*(vijf|5)\\b/gi, '1.5')\n        .replace(/\\btwee\\s*(komma|punt)\\s*(vijf|5)\\b/gi, '2.5')\n        .replace(/\\bdrie\\s*(komma|punt)\\s*(vijf|5)\\b/gi, '3.5')\n        .replace(/\\bvier\\s*(komma|punt)\\s*(vijf|5)\\b/gi, '4.5')\n        .replace(/(\\d+)\\s*(komma|punt)\\s*(\\d+)/gi, function(_, a, sep, b) { return a + '.' + b; })\n        .replace(/\\b(een|\u00e9\u00e9n)\\s+vijf\\b/gi, '1.5')\n        .replace(/\\btwee\\s+vijf\\b/gi, '2.5')\n        .replace(/\\bdrie\\s+vijf\\b/gi, '3.5')\n        .replace(/\\b(een\\s+half|\u00e9\u00e9n\\s+half|1\\s+half)\\b/gi, '0.5')\n        .replace(/\\b(twee\\s+half|2\\s+half)\\b/gi, '2.5')\n        .replace(/\\b(drie\\s+half|3\\s+half)\\b/gi, '3.5')\n        .replace(/\\bhalf\\b|\\bhalve\\b/gi, '0.5')\n        .replace(/\\b(lager|hoger|voorwaarts|achterwaarts|kraagplooi|omhoog|omlaag|plus|erbij|wijder|groter|kleiner|innemen|vernauwen|minder|meer|uitleggen|vergroten|eraf)\\b/gi, '')\n        .replace(/\\bnul\\b/gi, '0')\n        .replace(/\\b(\u00e9\u00e9n|een)\\b/gi, '1')\n        .replace(/\\btwee\\b/gi, '2')\n        .replace(/\\bdrie\\b/gi, '3')\n        .replace(/\\bvier\\b/gi, '4');\n\n      s = s.replace(/\\b(min|minus)\\s+(\\d+(\\.\\d+)?)/gi, function(_, sign, num) {\n        return '-' + num;\n      });\n\n      s = s.replace(/(\\d+),(\\d+)/g, '$1.$2');\n      var m = s.match(/-?\\d+(\\.\\d+)?/);\n      return m ? parseFloat(m[0]) : null;\n    }\n\n    function parseDirection(t, fieldName) {\n      var lbl = DIR_LABEL[fieldName];\n      if (lbl) {\n        var pos = lbl.pos.toLowerCase(), neg = lbl.neg.toLowerCase();\n        if (new RegExp('\\\\b' + neg.replace(/\\s+/g, '\\\\s+') + '\\\\b', 'i').test(t)) return -1;\n        if (new RegExp('\\\\b' + pos.replace(/\\s+/g, '\\\\s+') + '\\\\b', 'i').test(t)) return 1;\n        if (/\\blager\\b/i.test(t) && neg === 'lager') return -1;\n        if (/\\bhoger\\b/i.test(t) && pos === 'hoger') return 1;\n        if (/\\bkraagplooi\\b/i.test(t) && fieldName === 'Kraag') return -1;\n      }\n      if (/\\b(min|minus|eraf|kleiner|innemen|vernauwen|minder|omlaag)\\b/i.test(t)) return -1;\n      if (/\\b(plus|erbij|wijder|groter|uitleggen|vergroten|meer|omhoog)\\b/i.test(t)) return 1;\n      return 0;\n    }\n\n    function hasContradiction(text, fieldName, rawNum) {\n      if (rawNum === null || Math.abs(rawNum) < 0.001) return false;\n      var dir = parseDirection(text, fieldName);\n      if (dir === 0) return false;\n      var hasExplicitMinus = /\\b(min|minus|-)\\s*\\d/i.test(text);\n      var hasExplicitPlus = /\\b(plus|\\+)\\s*\\d/i.test(text);\n      if (dir === 1 && rawNum < 0 && hasExplicitMinus) return true;\n      if (dir === -1 && rawNum > 0 && hasExplicitPlus) return true;\n      return false;\n    }\n\n    function buildDisplay(cfgName, dir, finalVal, spokenText) {\n      var lbl = DIR_LABEL[cfgName];\n      var dirSpoken = dir !== 0 && lbl && (\n        new RegExp('\\\\b' + (dir === 1 ? lbl.pos : lbl.neg).toLowerCase() + '\\\\b', 'i').test(spokenText) ||\n        /\\b(min|minus|lager|hoger|kleiner|groter|voorwaarts|achterwaarts|omhoog|omlaag|kraagplooi)\\b/i.test(spokenText)\n      );\n      var name = cfgName;\n      if (/kraagplooi/i.test(spokenText) && cfgName === 'Kraag') name = 'Kraagplooi';\n      var d = name;\n      if (dirSpoken && lbl && !/kraagplooi/i.test(spokenText)) d += ' ' + (dir === 1 ? lbl.pos : lbl.neg);\n      if (finalVal !== null) d += ' ' + fmt(finalVal);\n      return d;\n    }\n\n    function findField(t) {\n      if (/kraagplooi/i.test(t) && !/kraag\\b/i.test(t.replace(/kraagplooi/gi, ''))) {\n        var kId = Object.keys(sliderState).find(function(k) { return sliderState[k].cfg.name === 'Kraag'; });\n        return kId !== undefined ? parseInt(kId, 10) : null;\n      }\n      for (var id in sliderState) {\n        if (sliderState[id].cfg.matchFn(t)) return parseInt(id, 10);\n      }\n      return null;\n    }\n\n    function resetPending() {\n      pendingIdx = null;\n      accText = '';\n      pendingSchouderVal = null;\n    }\n\n    function startGrace(idx) {\n      graceIdx = idx;\n      clearTimeout(graceTimer);\n      graceTimer = setTimeout(function() {\n        graceIdx = null;\n      }, 2500);\n    }\n\n    function clearGrace() {\n      graceIdx = null;\n      clearTimeout(graceTimer);\n      graceTimer = null;\n    }\n\n    function exitFocus() {\n      cancelApplyDebounce();\n      resetPending();\n      clearGrace();\n      hideOverlay();\n      setTranscript('');\n    }\n\n    voiceOverlay.addEventListener('click', function(e) {\n      if (e.target === voiceOverlay) exitFocus();\n    });\n\n    voiceOverlay.addEventListener('touchend', function(e) {\n      if (e.target === voiceOverlay) {\n        e.preventDefault();\n        exitFocus();\n      }\n    }, { passive: false });\n\n    function applyValue(idx, finalVal) {\n      var vals = sliderState[idx].vals;\n      var min = vals[0], max = vals[vals.length - 1];\n      if (finalVal < min || finalVal > max) {\n        flashRow(idx, '#f59e0b');\n        return 'out-of-range';\n      }\n      var sw = sliderState[idx].swEl;\n      var curValText = sw ? sw.textContent : '0';\n      var curVal = curValText === '0' ? 0 : parseFloat(curValText);\n      previousValues[idx] = isNaN(curVal) ? 0 : curVal;\n      sliderState[idx].snapToValue(finalVal, 1000);\n      lastAppliedIdx = idx;\n      // PAON data hook (ADR-052) \u2014 the single line added to the founder's\n      // source. Emits the applied value so React can persist it; changes\n      // no behaviour of the widget itself.\n      root.dispatchEvent(new CustomEvent('vox:apply', {\n        detail: { field: sliderState[idx].cfg.name, value: finalVal },\n        bubbles: true\n      }));\n      setTimeout(hideOverlay, 900);\n      setTimeout(function() { setTranscript(''); }, 1100);\n      return 'applied';\n    }\n\n    function scheduleApply(idx, finalVal, dir, cfg, spokenText, onApplied) {\n      if (!isWholeNumber(finalVal)) {\n        cancelApplyDebounce();\n        var result = applyValue(idx, finalVal);\n        if (result === 'applied' && onApplied) onApplied(idx);\n        return;\n      }\n      if (applyDebounceCtx && applyDebounceCtx.idx !== idx) cancelApplyDebounce();\n      applyDebounceCtx = { idx: idx, finalVal: finalVal, dir: dir, cfg: cfg, spokenText: spokenText, onApplied: onApplied };\n      clearTimeout(applyDebounceTimer);\n      applyDebounceTimer = setTimeout(function() {\n        applyDebounceTimer = null;\n        var ctx = applyDebounceCtx;\n        applyDebounceCtx = null;\n        if (!ctx) return;\n        var result = applyValue(ctx.idx, ctx.finalVal);\n        if (result === 'applied' && ctx.onApplied) ctx.onApplied(ctx.idx);\n      }, 750);\n    }\n\n    function tryRefinePendingDebounce(chunk) {\n      if (!applyDebounceCtx) return false;\n      var ctx = applyDebounceCtx;\n      var combined = ctx.spokenText + ' ' + chunk;\n      var refined = parseValue(combined);\n      if (refined !== null && !isWholeNumber(refined)) {\n        cancelApplyDebounce();\n        var dir = parseDirection(combined, ctx.cfg.name);\n        if (dir === 0) dir = refined >= 0 ? 1 : -1;\n        var finalVal = dir === -1 ? -Math.abs(refined) : Math.abs(refined);\n        setTranscript(buildDisplay(ctx.cfg.name, dir, finalVal, combined));\n        showOverlay(ctx.idx);\n        var result = applyValue(ctx.idx, finalVal);\n        if (result === 'applied') {\n          resetPending();\n          startGrace(ctx.idx);\n          isLocked = true;\n          rec.stop();\n          setTimeout(function() {\n            isLocked = false;\n            if (voiceActive) rec.start();\n          }, 1200);\n        }\n        return true;\n      }\n      return false;\n    }\n\n    function undoLast() {\n      if (lastAppliedIdx === null) return;\n      var idx = lastAppliedIdx;\n      var prev = previousValues[idx] !== undefined ? previousValues[idx] : 0;\n      lastAppliedIdx = null;\n      showOverlay(idx);\n      setTranscript(sliderState[idx].cfg.name + ' \u2192 ' + fmt(prev));\n      sliderState[idx].snapToValue(prev, 600);\n      setTimeout(function() {\n        hideOverlay();\n        setTranscript('');\n      }, 1200);\n    }\n\n    function handleGraceChunk(chunk) {\n      if (graceIdx === null) return false;\n      var idx = graceIdx;\n      if (/\\bnee\\b/i.test(chunk)) {\n        clearGrace();\n        showOverlay(idx);\n        pendingIdx = idx;\n        accText = '';\n        setTranscript(sliderState[idx].cfg.name + ' \u2026');\n        return true;\n      }\n      var num = parseValue(chunk);\n      if (num !== null) {\n        var cfg = sliderState[idx].cfg;\n        var dir = parseDirection(chunk, cfg.name);\n        if (dir === 0) dir = num >= 0 ? 1 : -1;\n        var finalVal = dir === -1 ? -Math.abs(num) : Math.abs(num);\n        if (hasContradiction(chunk, cfg.name, num)) {\n          clearGrace();\n          showOverlay(idx);\n          flashRow(idx, '#f59e0b');\n          pendingIdx = idx;\n          accText = chunk;\n          return true;\n        }\n        clearGrace();\n        showOverlay(idx);\n        setTranscript(buildDisplay(cfg.name, dir, finalVal, chunk));\n        scheduleApply(idx, finalVal, dir, cfg, chunk, function(appliedIdx) {\n          startGrace(appliedIdx);\n          isLocked = true;\n          rec.stop();\n          setTimeout(function() {\n            isLocked = false;\n            if (voiceActive) rec.start();\n          }, 1200);\n        });\n        return true;\n      }\n      return false;\n    }\n\n    function voxToggleRecording() {\n      var SR = window.SpeechRecognition || window.webkitSpeechRecognition;\n      if (!SR) {\n        setTranscript('Niet ondersteund');\n        return;\n      }\n\n      if (!rec) {\n        rec = new SR();\n        rec.lang = 'nl-NL';\n        rec.interimResults = true;\n        rec.continuous = true;\n\n        rec.onresult = function(e) {\n          if (isLocked) return;\n\n          for (var i = e.resultIndex; i < e.results.length; ++i) {\n            var chunk = e.results[i][0].transcript.toLowerCase().trim();\n\n            if (tryRefinePendingDebounce(chunk)) {\n              if (isLocked) return;\n              continue;\n            }\n\n            if (graceIdx !== null) {\n              if (handleGraceChunk(chunk)) {\n                if (isLocked) return;\n                continue;\n              }\n              clearGrace();\n            }\n\n            if (/\\b(stop|stoppen|weg|escape|klaar|cancel|afsluiten)\\b/i.test(chunk)) {\n              exitFocus();\n              continue;\n            }\n\n            var isUndo = /\\b(terug|verwijder(en)?|annuleer(en)?|ga\\s+terug|niet\\s+goed|fout|herstel|corrigeer|wis|ongedaan|laatste|vorige)\\b/i.test(chunk);\n            if (isUndo) {\n              if (pendingIdx === null && pendingSchouderVal === null) undoLast();\n              continue;\n            }\n\n            if (/\\bnee\\b/i.test(chunk) && (pendingIdx !== null || overlayActiveIdx !== null)) {\n              var keepIdx = pendingIdx !== null ? pendingIdx : overlayActiveIdx;\n              accText = '';\n              pendingIdx = keepIdx;\n              setTranscript(sliderState[keepIdx].cfg.name + ' \u2026');\n              continue;\n            }\n\n            if (pendingIdx !== null) {\n              var chunkForField = chunk.replace(/-?\\d+(\\.\\d+)?/g, '').trim();\n              var newField = chunkForField ? findField(chunkForField) : null;\n              var hasNewSchouder = /\\bschouders?\\b/i.test(chunk);\n\n              if (newField !== null && newField !== pendingIdx) {\n                cancelApplyDebounce();\n                resetPending();\n                pendingIdx = newField;\n                accText = chunk;\n                setTranscript(sliderState[newField].cfg.name + ' \u2026');\n                showOverlay(newField);\n                continue;\n              }\n\n              if (hasNewSchouder && parseValue(chunk) === null && !/\\brechts?\\b|\\blinks?\\b/i.test(chunk)) {\n              } else {\n                accText += ' ' + chunk;\n              }\n\n              var numA = parseValue(accText);\n              if (numA !== null) {\n                var cfgA = sliderState[pendingIdx].cfg;\n                var dirA = parseDirection(accText, cfgA.name);\n                if (dirA === 0) dirA = numA >= 0 ? 1 : -1;\n                var finalValA = dirA === -1 ? -Math.abs(numA) : Math.abs(numA);\n                setTranscript(buildDisplay(cfgA.name, dirA, finalValA, accText));\n\n                if (hasContradiction(accText, cfgA.name, numA)) {\n                  flashRow(pendingIdx, '#f59e0b');\n                  accText = '';\n                  continue;\n                }\n\n                var capturedIdx = pendingIdx;\n                var capturedAccText = accText;\n                var capturedCfgA = cfgA;\n                var capturedDirA = dirA;\n\n                scheduleApply(capturedIdx, finalValA, capturedDirA, capturedCfgA, capturedAccText, function(appliedIdx) {\n                  resetPending();\n                  startGrace(appliedIdx);\n                  isLocked = true;\n                  rec.stop();\n                  setTimeout(function() {\n                    isLocked = false;\n                    if (voiceActive) rec.start();\n                  }, 1200);\n                });\n\n                accText = '';\n              } else {\n                var cfgA2 = sliderState[pendingIdx].cfg;\n                var dirA2 = parseDirection(accText, cfgA2.name);\n                var lblA2 = DIR_LABEL[cfgA2.name];\n                var displayA = cfgA2.name;\n\n                if (dirA2 !== 0 && lblA2) {\n                  var dwp = (\n                    new RegExp('\\\\b' + (dirA2 === 1 ? lblA2.pos : lblA2.neg).toLowerCase() + '\\\\b', 'i').test(accText) ||\n                    /\\b(min|minus|lager|hoger|kleiner|groter|voorwaarts|achterwaarts|omhoog|omlaag|kraagplooi)\\b/i.test(accText)\n                  );\n                  if (dwp) displayA += ' ' + (dirA2 === 1 ? lblA2.pos : lblA2.neg);\n                }\n\n                setTranscript(displayA + ' \u2026');\n              }\n              continue;\n            }\n\n            if (pendingSchouderVal !== null) {\n              var isR = /\\brechts?\\b/i.test(chunk);\n              var isL = /\\blinks?\\b|\\bkinks\\b/i.test(chunk);\n              var isBoth = /\\bbeide\\b|\\ballebei\\b|\\balle\\s*twee\\b|\\blinks\\s+en\\s+rechts\\b|\\brechts\\s+en\\s+links\\b/i.test(chunk);\n\n              if (isBoth || (isR && isL)) {\n                var rIdx = parseInt(Object.keys(sliderState).find(function(k) { return sliderState[k].cfg.name === 'Schouder R'; }), 10);\n                var lIdx = parseInt(Object.keys(sliderState).find(function(k) { return sliderState[k].cfg.name === 'Schouder L'; }), 10);\n                var valB = pendingSchouderVal;\n                pendingSchouderVal = null;\n                setTranscript('Schouder L+R ' + fmt(valB));\n                showOverlayBoth(rIdx, lIdx);\n                applyValue(rIdx, valB);\n                applyValue(lIdx, valB);\n                setTimeout(hideOverlay, 900);\n                isLocked = true;\n                rec.stop();\n                setTimeout(function() {\n                  isLocked = false;\n                  if (voiceActive) rec.start();\n                }, 1200);\n                return;\n              }\n\n              if (isR || isL) {\n                var sideName = isR ? 'Schouder R' : 'Schouder L';\n                var sideIdx = parseInt(Object.keys(sliderState).find(function(k) { return sliderState[k].cfg.name === sideName; }), 10);\n                var valS = pendingSchouderVal;\n                pendingSchouderVal = null;\n                setTranscript(sideName + ' ' + fmt(valS));\n                showOverlay(sideIdx);\n                var resultS = applyValue(sideIdx, valS);\n                if (resultS === 'applied') {\n                  startGrace(sideIdx);\n                  isLocked = true;\n                  rec.stop();\n                  setTimeout(function() {\n                    isLocked = false;\n                    if (voiceActive) rec.start();\n                  }, 1200);\n                  return;\n                }\n                continue;\n              }\n\n              setTranscript('Welke schouder?');\n              continue;\n            }\n\n            var hasSchouders = /\\bschouders\\b/i.test(chunk) || /\\bbeide\\s+schouder/i.test(chunk) || /\\bschouder\\s+beide\\b/i.test(chunk);\n            var hasSchouder = /\\bschouder\\b/i.test(chunk);\n            var hasSide = /\\brechts?\\b|\\blinks?\\b|\\bkinks\\b/i.test(chunk);\n            var schouderNum = (hasSchouder || hasSchouders) ? parseValue(chunk) : null;\n            var schouderDir = 0;\n\n            if (hasSchouder || hasSchouders) {\n              if (/\\blager\\b/i.test(chunk)) schouderDir = -1;\n              else if (/\\bhoger\\b/i.test(chunk)) schouderDir = 1;\n              else schouderDir = parseDirection(chunk, 'Schouder R');\n            }\n\n            if (hasSchouders && schouderNum !== null) {\n              var dir2 = schouderDir !== 0 ? schouderDir : (schouderNum >= 0 ? 1 : -1);\n              var fv2 = dir2 === -1 ? -Math.abs(schouderNum) : Math.abs(schouderNum);\n              var rIdx2 = parseInt(Object.keys(sliderState).find(function(k) { return sliderState[k].cfg.name === 'Schouder R'; }), 10);\n              var lIdx2 = parseInt(Object.keys(sliderState).find(function(k) { return sliderState[k].cfg.name === 'Schouder L'; }), 10);\n              setTranscript('Schouder L+R ' + fmt(fv2));\n              showOverlayBoth(rIdx2, lIdx2);\n              applyValue(rIdx2, fv2);\n              applyValue(lIdx2, fv2);\n              setTimeout(hideOverlay, 900);\n              isLocked = true;\n              rec.stop();\n              setTimeout(function() {\n                isLocked = false;\n                if (voiceActive) rec.start();\n              }, 1200);\n              return;\n            }\n\n            if (hasSchouder && !hasSide && schouderNum !== null) {\n              var dir3 = schouderDir !== 0 ? schouderDir : (schouderNum >= 0 ? 1 : -1);\n              var fv3 = dir3 === -1 ? -Math.abs(schouderNum) : Math.abs(schouderNum);\n              pendingSchouderVal = fv3;\n              setTranscript('Welke schouder?');\n              continue;\n            }\n\n            if (hasSchouder && !hasSide && schouderNum === null && !hasSchouders) continue;\n\n            var matchIdx = findField(chunk);\n            if (matchIdx === null) continue;\n\n            var cfg = sliderState[matchIdx].cfg;\n            var num = parseValue(chunk);\n            var dir = parseDirection(chunk, cfg.name);\n            if (dir === 0 && num !== null) dir = num >= 0 ? 1 : -1;\n            var finalVal = num !== null ? (dir === -1 ? -Math.abs(num) : Math.abs(num)) : null;\n\n            setTranscript(buildDisplay(cfg.name, dir, finalVal, chunk));\n            showOverlay(matchIdx);\n\n            if (finalVal === null) {\n              pendingIdx = matchIdx;\n              accText = chunk;\n              continue;\n            }\n\n            if (hasContradiction(chunk, cfg.name, num)) {\n              flashRow(matchIdx, '#f59e0b');\n              pendingIdx = matchIdx;\n              accText = chunk;\n              continue;\n            }\n\n            var capturedMatchIdx = matchIdx;\n            var capturedCfg = cfg;\n            var capturedDir = dir;\n            var capturedChunk = chunk;\n\n            scheduleApply(capturedMatchIdx, finalVal, capturedDir, capturedCfg, capturedChunk, function(appliedIdx) {\n              resetPending();\n              startGrace(appliedIdx);\n              isLocked = true;\n              rec.stop();\n              setTimeout(function() {\n                isLocked = false;\n                if (voiceActive) rec.start();\n              }, 1200);\n            });\n          }\n        };\n\n        rec.onend = function() {\n          if (voiceActive && !isLocked) rec.start();\n        };\n\n        rec.onerror = function(e) {\n          if (e.error === 'not-allowed' || e.error === 'service-not-allowed') {\n            voiceActive = false;\n            var pill = root.querySelector('.vox-pill');\n            var btn = root.querySelector('.vox-record-btn');\n            if (pill) pill.classList.remove('listening');\n            if (btn) btn.classList.remove('listening');\n            setTranscript('Microfoon geweigerd');\n          }\n        };\n      }\n\n      voiceActive = !voiceActive;\n      var pill = root.querySelector('.vox-pill');\n      var btn = root.querySelector('.vox-record-btn');\n\n      if (pill) pill.classList.toggle('listening', voiceActive);\n      if (btn) btn.classList.toggle('listening', voiceActive);\n\n      if (voiceActive) {\n        setTranscript('');\n        resetPending();\n        rec.start();\n      } else {\n        cancelApplyDebounce();\n        rec.stop();\n        setTranscript('');\n        hideOverlay();\n        resetPending();\n        clearGrace();\n      }\n    }\n\n    setTimeout(function() {\n      var btn = root.querySelector('.vox-record-btn');\n      if (!btn || !window.IntersectionObserver) return;\n      var obs = new IntersectionObserver(function(entries) {\n        if (!entries[0].isIntersecting && voiceActive) voxToggleRecording();\n      }, { threshold: 0 });\n      obs.observe(btn);\n    }, 300);\n  });\n})();\n";
